{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Hello v2",
  "description": "Initial handshake payload sent by the mod to the daemon.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "game_version",
    "mod_version",
    "schema_version",
    "supported_protocol_versions"
  ],
  "properties": {
    "game_version": {
      "type": "string",
      "minLength": 1
    },
    "mod_version": {
      "type": "string",
      "minLength": 1
    },
    "schema_version": {
      "type": "string",
      "const": "v2"
    },
    "supported_protocol_versions": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": ["v2"]
      }
    },
    "auth_token": {
      "type": "string",
      "minLength": 1,
      "description": "Optional shared secret for daemon handshake authentication."
    }
  }
}
